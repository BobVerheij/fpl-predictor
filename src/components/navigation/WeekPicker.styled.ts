import styled from "styled-components";

export const WeekPicker = styled.select`
  color: white;
  font-weight: 800;
  text-align: left;
  font-size: 0.8rem;
  position: relative;
  align-self: center;

  background-color: transparent;
  border: 2px solid var(--secondary);
  border-radius: 100px;
  height: 2rem;
  padding: 0 1rem;
  outline: 0;

  -webkit-appearance: none;
  -moz-appearance: none;
  appearance: none;
`;
